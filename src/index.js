/**
 * @description Главный файл, в котором происходит рендеринг App в файле index.html
 */

import React from "react";
import ReactDOM from "react-dom";
import App from "./App"

ReactDOM.render(
    <App/>,
    document.getElementById("root")
);